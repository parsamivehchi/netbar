# XcodeGen output (regenerate with `xcodegen generate`)
*.xcodeproj

# Build artifacts
build/
DerivedData/

# macOS
.DS_Store

# local session archives + breadcrumbs (fold into a PR when convenient)
progress/
.claude/
